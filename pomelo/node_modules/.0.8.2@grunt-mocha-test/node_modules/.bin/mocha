#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.1.14.0@mocha/bin/mocha" "$@"
  ret=$?
else 
  node  "$basedir/../../../.1.14.0@mocha/bin/mocha" "$@"
  ret=$?
fi
exit $ret
